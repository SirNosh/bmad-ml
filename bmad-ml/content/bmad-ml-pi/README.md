# bmad-ml-pi

pi-native BMad ML distribution with isolated specialist delegation.

Includes:
- Full BMad skill tree
- pi extension for `bmad_task`
- Curated prompt templates
- Hybrid runtime assets for Claude Code and Cursor

Install via the `bmad-ml` CLI using `--pi`, `--cc-pi`, or `--cursor-pi`.
