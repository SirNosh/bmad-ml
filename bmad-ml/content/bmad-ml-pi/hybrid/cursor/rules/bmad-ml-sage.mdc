---
description: Invoke bmad-ml sage (pi-backed). Use only when Nosh routes a matching task.
---

Build the curated payload, run `node .bmad-ml/dispatch-pi.mjs sage <prompt-file>`, and summarize streamed SubagentEvents.
