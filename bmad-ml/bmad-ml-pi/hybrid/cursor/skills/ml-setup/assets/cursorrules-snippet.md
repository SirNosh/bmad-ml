# BMad ML — {project_name}

## Module Info
- ML module is active — config at `_bmad/config.yaml` (section: `ml`)
- Primary entry point: invoke `bmad-ml-nosh` for all ML tasks
- Framework: {ml_framework} | Tracker: {experiment_tracker}

## Division Routing
- **Research, experiments, model development** → AI Lab (autonomous execution)
- **AI products, LLM apps, deployment** → AI Startup (hands-on collaborative)

## Artifact Locations
- Planning artifacts: `{planning_artifacts}`
- Experiment artifacts: `{experiment_artifacts}`
- Research & references: `{project_knowledge}`

## Conventions
- Always load module config before starting any ML workflow
- Use iteration-log.yaml to track experiment cycles
- Experiment status tracked in experiment-status.yaml
